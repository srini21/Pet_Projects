#include<iostream>
#include<vector>
#include<stack>
using namespace std;

int main(){
  vector<stack<int > vec;
  for(int i=0;i<5;i++){
    stack<int> s;
    for(int j=0;j<10;j++){
      s.push(j);
    }
    vec.push(s);
  }
}
